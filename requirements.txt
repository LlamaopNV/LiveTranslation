# Core dependencies for Live Translation System
# Python 3.10+ required

# IMPORTANT: Install PyTorch with CUDA first:
# For RTX 5080 (sm_120/Blackwell): pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu130
# For older GPUs: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

# Text-to-Speech (German)
TTS  # Coqui TTS for local German speech synthesis

# GPU-accelerated Streaming ASR
faster-whisper==1.2.0
torch>=2.0.0
torchaudio>=2.0.0

# Audio I/O
sounddevice==0.4.6
scipy>=1.10.0
pydub>=0.25.1  # For MP3 export
pyaudiowpatch>=0.2.12.2  # WASAPI loopback for Windows system audio capture

# Voice Activity Detection
silero-vad==5.1

# Translation
transformers>=4.30.0
sentencepiece>=0.1.99
sacremoses>=0.0.53

# GUI
PyQt6==6.6.1

# Utilities
python-dotenv==1.0.0

# Performance optimizations
huggingface_hub[hf_xet]>=0.16.0  # Faster model downloads from Hugging Face
